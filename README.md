# Typing Diary — Frontend

Static frontend for Typing Diary.

## Deploy to Vercel

Import this folder as its own Vercel project. No build command is required.

Before deploying, edit `config.js` and set `window.TYPING_DIARY_API_URL` to the deployed backend URL.
